%h1 Listing keys

%table
  %tr
    %th Shop
    %th Identifier
    %th
    %th
    %th

  - @keys.each do |key|
    %tr
      %td= key.shop.name
      %td= key.identifier
      %td= link_to 'Show', key
      %td= link_to 'Edit', edit_key_path(key)
      %td= link_to 'Destroy', key, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Key', new_key_path
